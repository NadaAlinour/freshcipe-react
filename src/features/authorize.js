/* set authcontext with authenticated user although i'm not really sure what 
 like do we save the user's name or like an id or a key or something
 set isLoggedIn */
 // check if keep me logged in is checked
//login
export const login = () => {
  console.log("this is login function");
};

/* clear authcontext and isLoggedIn to false
 remove user from localstorage (or cookie sessions idk what to use)*/
 // use for any logout buttons 
//logout
export const logout = () => {
  console.log("this is logout function");
};
